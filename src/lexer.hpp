#pragma once

#include <string>
#include <vector>

class Lexer {
public:
    // Tokenize Lua source into a vector of tokens (placeholder signature)
    std::vector<std::string> tokenize(const std::string& src);
};
